sp {propose*split
   (state <s> ^name  BJ_OS
              ^io.input-link.split 1
             -^io.input-link.chips 1)
-->
   (<s> ^operator <o> +)
   (<o> ^name split
        ^actions.loopDecision t)}